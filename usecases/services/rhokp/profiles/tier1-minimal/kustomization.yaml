apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rhokp

labels:
  - pairs:
      app.kubernetes.io/part-of: rhokp
      tier: "1"

resources:
  - ../../manifests/base
  - ../../manifests/server
