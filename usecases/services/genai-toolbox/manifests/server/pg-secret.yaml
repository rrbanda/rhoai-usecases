apiVersion: v1
kind: Secret
metadata:
  name: genai-toolbox-pg-credentials
  labels:
    app.kubernetes.io/name: genai-toolbox
    app.kubernetes.io/component: config
  annotations:
    description: >-
      PostgreSQL credentials for genai-toolbox MCP server.
      Update PG_USER and PG_PASSWORD for your environment.
      For production, use SealedSecrets or ExternalSecrets instead.
type: Opaque
stringData:
  PG_USER: llamastack
  PG_PASSWORD: CHANGE_ME
